# Dependências de produção — espelho de pyproject.toml [project.dependencies]
# Atualizar aqui quando pyproject.toml mudar.
sqlalchemy==2.0.50
psycopg2-binary==2.9.11
alembic==1.18.4
pandas>=2.2.0
requests>=2.33.1
# Security: pin vulnerable transitive deps (Dependabot alerts #1-#4)
urllib3>=2.7.0
mako>=1.3.12
